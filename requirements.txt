# FastAPI 及其相关（核心 web 框架）
fastapi>=0.115.0          # 最新系列，兼容 3.12
uvicorn[standard]>=0.34.0 # 推荐带 standard extras，支持 HTTP/2 等
python-multipart>=0.0.20  # 用于文件上传

# Pydantic 设置管理
pydantic-settings>=2.8.0  # 最新，兼容 3.12+

# LangChain 生态（核心 + 集成包）
langchain-openai>=1.1.11     # 最新 2026-03 发布，兼容 Python >=3.10
langchain-huggingface>=1.2.1 # 最新 2026-03，HuggingFace 集成
langchain-chroma>=1.1.0      # 最新 2025-12，Chroma 向量存储集成
langchain-community>=0.4.1   # 最新 2025-10，社区 loader 等（包含 PyPDFLoader 等）

# PDF & DOCX 解析（你当前使用的 loader 依赖）
pypdf>=6.9.1                 # 最新 2026-03，纯 Python PDF 处理，兼容 3.12
docx2txt>=0.8                # 较稳定版本，基本没大更新，但兼容 3.12